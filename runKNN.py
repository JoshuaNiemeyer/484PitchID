import importData

# runs the KNN on the baseball training data
# uses Euclidean, Manhattan, and Hamming distances
# prints accuracy, precision, recall, and F1 scores each distance type
# expect ~10-15min of total processing time for running all types.
# KNNresults.png has a sample of its output
importData.testImport()